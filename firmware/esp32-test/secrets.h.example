// Copia este archivo como secrets.h (ese SI esta en .gitignore) y pon tus
// valores reales ahi. NUNCA subas secrets.h a Git.

#define WIFI_SSID "tu-red-wifi"
#define WIFI_PASSWORD "tu-password-wifi"

// Secreto del dispositivo NODO-ESP32-01, emitido una sola vez (D-003).
// Si se pierde o se sospecha que se filtro, hay que rotarlo (nueva fila en
// device_credentials, revocando la anterior) -- no hay forma de recuperarlo,
// solo esta almacenado como hash en la base de datos.
#define DEVICE_SECRET "pega-aqui-el-secreto-que-te-dieron"
