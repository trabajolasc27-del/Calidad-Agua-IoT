// Entorno de PRODUCCION. Se deja sin valores reales a proposito: la
// configuracion real de despliegue se define cuando se decida el hosting
// (Fase 5, ver docs/DECISIONS.md D-009), nunca hardcodeada aqui.
export const environment = {
  production: true,
  supabaseUrl: '',
  supabaseAnonKey: '',
};
